const readline = require('readline')
const rl = readline.createInterface({
    input: process.stdin,
    output: process.stdout
})

const array = [];

rl.on('line', function (line) {
    array.push(Number(line))
})
    .on('close', function () {

        //문자열
        array[0];
        //인덱스
        array[1];

        let printIndexOfStrings = function (inputStrings, inputIndex) {
            inputStrings[inputIndex]
        }


        process.exit()
    })
